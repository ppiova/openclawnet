{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },
  "OpenClawNet": {
    "Model": {
      "_comment": "Default Ollama model passed to the Gateway as Model__Model. Overridable via OPENCLAW_OLLAMA_MODEL env var.",
      "Default": "gemma4:e2b"
    },
    "Deploy": {
      "_comment": "Deploy target: 'docker' (default, generates docker-compose via `aspire publish`) or 'azure' (use `aspire publish --publisher azure-container-apps` or `azd up`)",
      "Target": "docker"
    }
  },
  "Storage": {
    "AgentsFolderName": "agents"
  }
}
